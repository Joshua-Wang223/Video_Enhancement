全部完成。代码块逻辑完整：开关默认行为与原来完全一致（_force_new 判定在默认 '0' 下不变），打印分支保留。

完成报告

执行内容（Phase 0 + Phase 1，均为零行为改变）：

┌──────────────────┬─────────────────────────────────────────────┬────────────────────────────────────────────────────────────────────────────────────────────────────┐
│       改动       │                    位置                     │                                                内容                                                │
├──────────────────┼─────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Phase 0 开关     │ external/ifrnet_video/main.py:1101-1104     │ IFRNET_NVENC_CROSS_SEGMENT_REUSE=1 放开 HEVC/AV1 跨段复用；默认 '0' 维持禁用，线上可一行回滚       │
├──────────────────┼─────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Phase 1 修复 R1  │ nvenc_sdk.py:1905-1909（_stream_begin）     │ _sps_pps_injected = False 段边界重置——LA>0 路径（段首 _stream_begin(force=True) 必经）             │
├──────────────────┼─────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Phase 1 修复 R1  │ nvenc_sdk.py:2399-2402（ce_pipeline f0 块） │ 同重置——LA=0 路径不经过 _stream_begin，以 _pending_f0_nv12 非空为段首标志（每段段首首批必暂存 f0） │
├──────────────────┼─────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Phase 1 注释修正 │ nvenc_sdk.py:3146-3147                      │ close() 内"跨段重置换"矛盾注释改为"关闭即弃"语义                                                   │
└──────────────────┴─────────────────────────────────────────────┴────────────────────────────────────────────────────────────────────────────────────────────────────┘

Sanity check：
- ✅ py_compile 两个文件通过（改后复检）
- ✅ verify_plan_implementation.py 90 项：83 PASS / 3 FAIL / 2 WARN / 2 SKIP
- ✅ 3 个 FAIL（BEH-B1/B3/B4）已确证与本次改动零关系：BEH-B 只执行 video_utils.split_video_by_time（不碰 ifrnet_video 代码）；Windows 复现实验证明失败原因是 09-01 修复 A 的 merge_trailing_fragment（video_utils.py:1136，末段 <2s 并入前段）使 9s 测试源产出 2 段，而 BEH-B 断言仍是旧的 len==3——验收脚本断言过期，08-31 基线报告（90 项 0 FAIL）在修复 A 之前。建议另行更新 BEH-B 断言或改用不触发合并的素材
- ✅ memory ifrnet-hevc-cross-segment-reuse-restore.md 新建 + MEMORY.md 索引更新，canonical/仓库双侧同步

待办（需 Linux 生产侧 GPU）： A/B 实测矩阵（hevc×{constqp/LA=0, vbr_hq/LA=8} × 开关 0/1 + h264 对照）、硬指标门禁与决策门（Δ<1% 或任一 FAIL → 维持禁用归档；全 PASS → 灰度翻默认），详见 memory 文件与计划文档。

result: HEVC/AV1 跨段编码器复用恢复评估完成——维持禁用默认，Phase 0（IFRNET_NVENC_CROSS_SEGMENT_REUSE 开关）+ Phase 1（_sps_pps_injected 段边界重置，含 LA=0 路径）已落地并验证，A/B 实测待 Linux GPU；另发现 BEH-B1/B3/B4 验收断言过期（P2-FIX-FRAG 末段合并所致，与本次改动无关）。