*.core
*.tar.gz
core.*
__pycache__/
*.pyc
.ipynb_checkpoints/
.vscode/
.idea/
.trt_cache/
.t2_cache/
_bak*
*.bak*
*_bak*
logs/
# [FIX-GITIGNORE-MODELS-ANCHOR] 原为未锚定的 `models/`、`models_*/` ——
# 未锚定模式会匹配**任意层级**的同名目录，实测把 external/IFRNet/models/
# （IFRNet 网络结构源码 IFRNet.py / IFRNet_S.py / IFRNet_L.py）一并忽略，
# 致其从未进仓库：全新 clone 缺该目录 → 导入/加载 IFRNet 架构必然失败
# （与 2026-09-23「No module named 'models'」生产事故同源）。
# 锚定到仓库根，只忽略根级权重/缓存目录，不动 external/ 下的源代码。
/models/
/models_*/
gfpgan/
output/
temp/
output*.txt
Video_Enhancement_github_token.txt

# [SEC-2026-09-23] cc-switch 配置文档内含真实 API Key（OpenRouter / DeepSeek）。
# 四个文件都在仓库根 config/ 下；首次推送被 GitHub Push Protection 拦下（GH013，
# OpenRouter Key）。注意 DeepSeek 的 sk- 密钥 GitHub **没有检测器**，只挡
# OpenRouter 会让 DeepSeek 密钥静默进仓，故整体排除。
config/cc-switch-*.md

# ── 运行日志 / 临时转储（2026-09-23 清理，避免 git status 刷屏）──
# 注：force_push_github.sh、tar_excludes.txt 属同步工作流工具，保留 tracked（不忽略）
and
Found_Cropped.txt
*.err
conversation-*.txt
tmp/
tests/validation_report_final.txt

# 工具/会话本地配置，不入库
.codebuddy/

# Plan/ 下的会话转储与历史快照（保留 *.md 设计/立项文档）
Plan/history_*
Plan/*session*
Plan/conversation*
Plan/Conversation-*
Plan/2026-*
Plan/*.txt
